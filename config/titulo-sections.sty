%%%%
%%  Definindo o nível máximo de subseção a ser contabilizado
%%%%
\setcounter{secnumdepth}{7} % Ao escolher 5, no caso da documentclass book, até os paragraph são numerados, porém, como foram adicionados itens a mais na hierarquia de seção (apêndices e anexos), precisa somar ao valor o número de itens adicionados. Isso acontece porque os apêndices e anexos são criados em um nível abaixo de \part, que não encapsula igual os \chapter

%Exemplo: \titleformat{command}[shape]{format}{label}{sep}{before-code}[after-code]

%%%%
%%  Configurando a formatação do título de seção
%%%%
%Exemplo: \titleformat{command}[shape]{format}{label}{sep}{before-code}[after-code]
%Seção primária
\titleformat{\chapter}[block]
{\normalfont\normalsize\bfseries}{\thechapter\space}{0ex}{\MakeUppercase}

%Seção secundária
\titleformat{\section}[block]
{\normalfont\normalsize\MakeUppercase}{\thesection\space}{0ex}{\MakeUppercase}

%Seção terciária
\titleformat{\subsection}[block]
{\normalfont\normalsize\bfseries}{\thesubsection\space}{0ex}{}

%Seção quaternária
\titleformat{\subsubsection}[block]
{\normalfont\normalsize\itshape}{\thesubsubsection\space}{0ex}{}

%Seção quinária
\titleformat{\paragraph}[block]
{\normalfont\normalsize}{\theparagraph\space}{0ex}{}


%%%%
%%  Configurando o espaçamento entre título de seção e texto
%%%%
%Seção primária
\titlespacing* % starred version: first paragraph is not indented
    {\chapter} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>

%Seção secundária
\titlespacing* % starred version: first paragraph is not indented
    {\section} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>

%Seção terciária
\titlespacing* % starred version: first paragraph is not indented
    {\subsection} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>

%Seção quaternária
\titlespacing* % starred version: first paragraph is not indented
    {\subsubsection} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>

%Seção quinária
\titlespacing* % starred version: first paragraph is not indented
    {\paragraph} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>


%%%%
%%  Configurando a apresentação e espaçamento de apêndices e anexos
%%%%
\titleformat{\apendice}[block]
  {\normalfont\normalsize\bfseries}{APÊNDICE \theapendice\space --\space}{0ex}{\MakeUppercase}
\titlespacing* % starred version: first paragraph is not indented
    {\apendice} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>

\titleformat{\anexo}[hang]
  {\normalfont\normalsize\bfseries}{ANEXO \theapendice\space --\space}{0ex}{\MakeUppercase}
\titlespacing* % starred version: first paragraph is not indented
    {\anexo} % <command>
    {0cm} % <left>
    {\baselineskip} % <before-sep>
    {\baselineskip} % <after-sep>